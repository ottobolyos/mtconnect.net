<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>MTConnect.NET_Generator_Tests</RootNamespace>
    <Nullable>enable</Nullable>

    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="NUnit" Version="3.14.0" />
    <PackageReference Include="NUnit3TestAdapter" Version="4.6.0" />
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
  </ItemGroup>

  <!--
    Build-only dependency on the generator project so `dotnet test` on this
    project transitively builds the generator into its own bin/, which the
    byte-identical-regen test then invokes via `dotnet run`. The
    ReferenceOutputAssembly=false switch keeps the generator's assembly out of
    the test process's reference graph — the tests treat the generator as an
    external CLI, not a library dependency.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\build\MTConnect.NET-SysML-Import\MTConnect.NET-SysML-Import.csproj" ReferenceOutputAssembly="false" />
  </ItemGroup>

  <!--
    Shared-source link on SourceStripper.cs so SourceStripperTests can unit-
    test the comment-stripper directly without linking the generator
    executable's assembly. The generator project compiles SourceStripper into
    its own bin/; the test project independently compiles the SAME source
    into ITS own bin/ under the linked path SourceStripper.cs. Both
    compilations produce equivalent code — the only observable difference
    is which assembly hosts the type.
  -->
  <ItemGroup>
    <Compile Include="..\..\build\MTConnect.NET-SysML-Import\SourceStripper.cs" Link="SourceStripper.cs" />
  </ItemGroup>

</Project>
